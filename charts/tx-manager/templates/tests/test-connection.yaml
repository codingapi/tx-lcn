apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tx-manager.fullname" . }}-test-connection"
  labels:
{{ include "tx-manager.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "tx-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
